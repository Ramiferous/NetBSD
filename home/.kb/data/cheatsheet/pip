## To install python apps using pip first install py##-pip (or py38-pip depending on your python version). 
But first, you'll then need to install pkg_alternatives before installing pip

$ doas pkgin install pkg_alternatives

$ doas pkgin install py38-pip py38-sqlite3

