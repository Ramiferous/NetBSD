# set a cronjob to reload your weather script for your bar
# run:

$ crontab -e

Add:

# run weather script for bar every 30 minutes                                   
30      *       *       *       *       sh $HOME/.scripts/weather.sh

# run moon phase script for bar ever 30 minutes
30      *       *       *       *       sh $HOME/.scripts/moon.sh

